/**
 * Vertex class and utilities for working with vertices.
 * Next step: Create interfaces so that we only use what we need.
 * Currently there is too much vertex data. Then we can use Graph<T extends V1 & V2> etc.,
 * where V1 and V2 are vertex interfaces.
 */
package com.codemelon.graph.vertex;